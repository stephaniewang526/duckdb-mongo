// No-op post-run script for conformance scenarios that need no harness-level
// capture: it prints nothing, so nothing is appended to the trace. Only the
// ordered-failure scenario replaces this with append_fail.js.
